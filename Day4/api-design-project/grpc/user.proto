syntax = "proto3";

service UserService {
  rpc GetUsers (Empty) returns (UserList);
}

message Empty {}

message User {
  string id = 1;
  string name = 2;
  string email = 3;
}

message UserList {
  repeated User users = 1;
}